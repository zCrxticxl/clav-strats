# CRA: don't auto-open a browser tab on `npm start` (Tauri opens its own window)
BROWSER=none

# Collab server URL baked into the build. Override at runtime via the ⚙ button.
# REACT_APP_COLLAB_URL=wss://your-tunnel.ngrok-free.app
