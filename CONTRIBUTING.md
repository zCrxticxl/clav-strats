# Contributing

Open an issue before proposing a substantial feature or map-data update. Keep changes focused and include screenshots or a short recording for editor/UI work.

Before opening a pull request, run:

```bash
npm ci
npm test -- --watchAll=false
npm run build
```

For collaboration, import/export, or updater changes, describe the tested failure path and how user data is preserved.
